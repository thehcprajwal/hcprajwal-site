GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile
PORT=3001
DOMAIN=hcprajwal.in

# n8n webhook — paste URL after setting up the Webhook node in n8n
N8N_WEBHOOK_URL=
N8N_WEBHOOK_SECRET=

# PostgreSQL (use @postgres:5432 in Docker, @localhost:5432 for local dev)
POSTGRES_USER=hcsystem
POSTGRES_PASSWORD=
POSTGRES_DB=hcsystem
DATABASE_URL=postgresql://hcsystem:password@postgres:5432/hcsystem

# Analytics dashboard password
ANALYTICS_PASSWORD=
IP_API_KEY=

# GitHub — resume repo (token only needed for private repos)
GITHUB_TOKEN=
GITHUB_OWNER=prajwalhc
GITHUB_RESUME_REPO=resume
